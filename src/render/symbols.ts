/**
 * Unicode glyphs used across the human output. No ASCII fallback for now; a
 * `NO_UNICODE` escape hatch could be added later if a terminal needs it.
 */

/** Success / done. */
export const CHECK = "✓"; // ✓
/** Failure. */
export const CROSS = "✗"; // ✗
/** Warning / attention. */
export const BANG = "!";
/** Continuation / leads-to. */
export const ARROW = "→"; // →
/** List bullet. */
export const BULLET = "•"; // •
